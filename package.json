{
  "name": "diff-view",
  "author": "lumine-code",
  "description": "A split pane diff tool.",
  "repository": "https://github.com/lumine-code/diff-view",
  "bugs": {
    "url": "https://github.com/lumine-code/diff-view/issues"
  },
  "keywords": [
    "git",
    "compare",
    "split-pane",
    "word-diff",
    "merge",
    "conflict"
  ],
  "version": "1.1.0",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "backgroundTips": [
    "{% if keys['diff-view:git-head'] %}You can diff the current file against its last commit with {{ 'diff-view:git-head' | keystroke }}{% else %}You can diff two files side by side, or the current file against its last commit.{% endif %}"
  ],
  "main": "./lib/diff-view",
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "diff": "^9.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  },
  "providedServices": {
    "diff-view": {
      "versions": {
        "1.0.0": "provideDiffView"
      }
    }
  },
  "configSchema": {
    "autoDiff": {
      "title": "Auto Diff",
      "description": "Automatically recalculates the diff when one of the editors changes.",
      "type": "boolean",
      "default": true
    },
    "diffWords": {
      "title": "Show Word Diff",
      "description": "Diffs the words between each line when this box is checked.",
      "type": "boolean",
      "default": true
    },
    "ignoreWhitespace": {
      "title": "Ignore Whitespace",
      "description": "Will not diff whitespace when this box is checked.",
      "type": "boolean",
      "default": false
    },
    "turnOffSoftWrap": {
      "title": "Disable Soft Wrap During Diff",
      "description": "Temporarily disables soft wrap during diff (restores when finished). Soft wrap is now fully supported, but you can enable this for the classic behavior.",
      "type": "boolean",
      "default": false
    },
    "muteNotifications": {
      "title": "Mute Notifications",
      "description": "Mutes all warning notifications when this box is checked.",
      "type": "boolean",
      "default": false
    },
    "hideDocks": {
      "title": "Hide Docks",
      "description": "Hides all docks (Tree View, Git, etc) during diff - shows when finished.",
      "type": "boolean",
      "default": false
    },
    "scrollSyncType": {
      "title": "Sync Scrolling",
      "description": "Syncs the scrolling of the editors.",
      "type": "string",
      "enum": [
        "Vertical + Horizontal",
        "Vertical",
        "None"
      ],
      "default": "Vertical + Horizontal"
    },
    "addedColorSide": {
      "title": "Added Color Side",
      "description": "The side that the latest version of the file is on. The added color will be applied to this editor and the removed color will be opposite.",
      "type": "string",
      "enum": [
        "left",
        "right"
      ],
      "default": "left"
    },
    "overrideThemeColors": {
      "title": "Override Highlight Colors",
      "description": "Override the line highlight colors (defined by variables in your selected syntax theme) with the colors selected below.",
      "type": "boolean",
      "default": false
    },
    "addedColor": {
      "title": "Added Custom Color",
      "description": "The color that will be used for highlighting added lines when **Override Highlight Colors** is checked.",
      "type": "color",
      "default": "green"
    },
    "removedColor": {
      "title": "Removed Custom Color",
      "description": "The color that will be used for highlighting removed lines when **Override Highlight Colors** is checked.",
      "type": "color",
      "default": "red"
    }
  }
}
